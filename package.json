{
  "name": "lenovo-turns-electron",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build": "electron-packager . --out=dist/win --platform=win32 --arch=x64 --icon=src/assets/images/lenovo-logo.png --asar --overwrite"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^1.7.9",
    "electron-packager": "^9.1.0"
  },
  "dependencies": {
    "jquery": "^3.2.1",
    "socket.io-client": "^2.0.4"
  }
}
